{
  "name": "postgres",
  "version": "0.0.3",
  "description": "Dockerized postgres for local dev -- not for use for production.",
  "author": "Brian Dombrowski <bdombro@gmail.com>",
  "license": "ISC",
  "private": true,
  "scripts": {
    "bootstrap": "yarn clean && bash tools/env-setup.sh && yarn start",
    "start": "docker-compose up -d",
    "dev": "yarn start",
    "test": "yarn start",
    "stop": "docker-compose stop",
    "clean": "yarn stop && rimraf db_data .env",
    "clean-docker": "docker stop $(docker ps -a -q); docker rm $(docker ps -a -q); docker volume prune -f; yarn clean",
    "cli": "psql -U $(dotenv -p PG_USERNAME) -h $(dotenv -p PG_CONNECTION)"
  },
  "dependencies": {
    "dotenv-cli": "^2.0.0",
    "rimraf": "^2.6.3"
  }
}
