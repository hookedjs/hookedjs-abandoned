import React, {Fragment} from "react";
import { Box, Text } from "rebass";

export type props = {};

export const Footer = ({}: props) => (<Fragment>
    <Box bg="pink">
      <Text>Footer</Text>
    </Box>
</Fragment>);
